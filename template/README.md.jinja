# {{ project_name }}

{{ project_description }}

## 技术栈

- Python {{ python_version }}
- FastAPI
- SQLAlchemy 2.x 异步 ORM
- Alembic
- Pydantic Settings
- uv
- Ruff、mypy、pytest、pre-commit

## 快速开始

```bash
cp .env.example .env
uv sync
uv run pytest
uv run uvicorn {{ package_name }}.main:app --reload
```

也可以使用 Makefile：

```bash
make sync
make run
make test
make lint
make typecheck
make check   # lint + typecheck + test + 编译检查
```

服务默认运行在 <http://localhost:{{ server_port }}>：

- 存活检查：`GET /livez`
- 就绪检查：`GET /readyz`
- Swagger UI：`/api/v1/docs`
- ReDoc：`/api/v1/redoc`
- OpenAPI JSON：`/api/v1/openapi.json`

示例接口 `POST /api/v1/examples/success`、`GET /api/v1/examples/items`
与 `POST /api/v1/examples/error` 仅在 `DEBUG=true` 时挂载，用于联调统一
单对象响应、分页列表响应与异常结构；默认 `.env` 中 `DEBUG=false`，示例接口
不会进入生产路由。

本地需要热重载时可设置 `SERVER_RELOAD=true`，仅在 `ENVIRONMENT=local`
或 `ENVIRONMENT=dev` 时生效。需要查看详细异常信息时可在本地设置
`DEBUG=true`，不要在共享环境或生产环境开启。

## HTTP 中间件

应用默认启用 CORS、GZip、TrustedHost 和请求上下文中间件。
GZip 仅在客户端 `Accept-Encoding` 支持 gzip 且响应体达到 `GZIP_MIN_SIZE`
时压缩响应；`GZIP_COMPRESS_LEVEL` 控制压缩级别。

`TRUSTED_HOSTS` 默认值为 `["*"]`，便于模板开箱即用。生产环境应改为真实域名，
例如 `["api.example.com","*.internal.example.com"]`，避免 Host Header 攻击。

## 响应与异常

HTTP 响应统一使用 `code/message/data/errors` 结构。成功响应使用
`{{ package_name }}.core.responses.success_response()`；分页列表使用
`{{ package_name }}.core.responses.paginated_response()` 和 `PaginatedData[T]`。
错误明细使用 `{{ package_name }}.core.responses.ErrorItem`（`field/message/code`），业务错误可抛出
`{{ package_name }}.core.exception.exceptions.AppException`。默认错误码来自
`{{ package_name }}.core.exception.error_codes.ErrorCode`：`0` 表示成功，
`100xxx` 为模板内置通用错误码，业务模块应扩展独立号段，不要直接复用 HTTP
状态码作为业务码。框架已统一处理业务异常、HTTP 异常、请求校验异常和未捕获
异常；未捕获异常在 `DEBUG=false` 时不会向客户端暴露内部错误信息。

`/api/v1/examples/success`、`/api/v1/examples/items` 和
`/api/v1/examples/error` 演示了如何用 Pydantic `BaseModel` 定义请求体、
单对象响应、分页列表响应、错误响应和字段级错误明细。示例 schema 放在
`{{ package_name }}/api/v1/examples/schema.py`，路由逻辑放在
`{{ package_name }}/api/v1/examples/router.py`，再由
`{{ package_name }}/api/v1/router.py` 聚合到 v1 API 中。

## 日志

应用使用 Loguru，并统一接管 Python 标准库、Uvicorn error 和 access 日志。
`LOG_FORMAT=auto` 时，本地、开发和测试环境输出便于阅读的文本，生产环境
输出 Loguru 原生 JSON，日志级别由 `LOG_LEVEL` 控制。

每个 HTTP 请求都会经过 `RequestContextMiddleware`，自动生成或透传
`X-Request-ID` 与 `X-Trace-ID`，并写入响应头和日志上下文。业务代码可通过
`{{ package_name }}.core.middleware.request_context.get_request_id()` 与
`get_trace_id()` 读取当前请求上下文。中间件会记录请求方法、路径、状态码和
耗时，不记录请求正文。

日志默认写入标准错误流，适合 Docker、Kubernetes 和日志采集平台。传统主机
部署可设置 `LOG_FILE_ENABLED=true`，每个进程会在 `LOG_FILE_DIR` 下分别生成
`application-{pid}.log` 和 `error-{pid}.log`，并按配置轮转、保留和压缩。
错误日志同时存在于两个文件中。目录权限为 `0700`，文件权限为 `0600`。

可通过 `logger.bind(order_id=order_id)` 添加结构化上下文。不要记录密码、令牌、
Authorization、Cookie、完整请求正文或其他敏感数据；模板不会自动脱敏日志正文
和 `extra` 字段。生产配置始终关闭 Loguru 的变量诊断输出。

## 数据库迁移

数据库会话依赖 `{{ package_name }}.db.session.SessionDep` 只负责提供
`AsyncSession`，并在异常时回滚；不会在请求结束后自动提交。写操作应在业务
服务或路由中显式 `await session.commit()`，或使用显式事务块。

模板不预置任何业务模型或迁移版本。添加模型并在
`{{ package_name }}/models/__init__.py` 导出后，通过命令生成迁移：

```bash
uv run alembic revision --autogenerate -m "add example table"
uv run alembic upgrade head
```

常用迁移命令也可通过 Makefile 执行：

```bash
make revision MESSAGE="add example table"
make migrate
make sql SQL_FILE=migration.sql
```

项目约定不使用数据库外键约束。跨表关联由 ORM 查询和业务逻辑维护。

## 健康检查

`GET /livez` 只检查应用进程是否存活，不访问外部依赖；`GET /readyz` 会执行
轻量数据库探测，适合作为容器 readiness probe、发布验证和负载均衡探测。
模板不注册旧式 `/health` 入口。

## Docker

```bash
docker build -t {{ project_slug }} .
docker run --rm -p {{ server_port }}:{{ server_port }} --env-file .env {{ project_slug }}
```

或使用：

```bash
make docker-build
make docker-run
```

镜像默认使用非 root 用户运行，并通过 `GET /readyz` 执行容器健康检查。
